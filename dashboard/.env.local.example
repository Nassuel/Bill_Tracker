# Python scraper API URL — start with: cd scraper && uvicorn api:app --reload
PYTHON_API_URL=http://localhost:8000

# Optional: override the path to bills.json (default: ../data/bills.json relative to dashboard/)
# DATA_FILE_PATH=
